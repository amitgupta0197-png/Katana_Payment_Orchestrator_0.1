<?xml version="1.0" encoding="utf-8"?>
<!-- One cell of the at-a-glance grid (merchant / via / power / queue). Deliberately quieter than
     the hero card it sits inside: these are supporting facts, not the headline. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="14dp" />
    <solid android:color="#0B1424" />
    <stroke android:width="1dp" android:color="@color/outline" />
</shape>
